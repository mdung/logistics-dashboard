import React from 'react';
import { Box, Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Welcome to the Logistics Management System</Typography>
      <Typography variant="body1">Use the navigation menu to access different sections of the system.</Typography>
    </Box>
  );
};

export default HomePage;
